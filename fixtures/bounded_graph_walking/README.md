# Bounded Graph Walking Fixture Matrix

This fixture is a deterministic synthetic graph contract for the Bounded Graph Walking / PathEvidence Latency phase.

It is not a benchmark and does not claim product recall, precision, or final intended performance. The fixture exists to lock expected traversal budgets, labels, evidence boundaries, relation-mode policies, and PathEvidence metadata before implementation changes.

The source of truth is `manifest.json`.
