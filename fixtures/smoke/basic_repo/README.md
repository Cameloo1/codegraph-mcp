# Basic Smoke Repo

This deterministic fixture is intentionally tiny. It exists to prove that the
`codegraph-mcp index` command can parse and index a supported source file without
requiring CGC, Autoresearch, network access, or external benchmark artifacts.
