# Dynamic Import Marked Heuristic

Trap: the import specifier is computed at runtime. A graph may record an unresolved heuristic import, but must not claim an exact edge to plugins/alpha unless a resolver proves that target.
