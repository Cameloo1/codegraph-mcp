# Benchmark Results

Generated benchmark results are local by default and ignored unless explicitly
promoted. Keep raw logs, predictions, patches, DBs, and model outputs out of
tracked docs.

Per-track generated results should now live under
`benchmarks/tracks/<track>/results/`. This aggregate directory remains for
full-run summaries and compatibility reports that intentionally combine tracks.
